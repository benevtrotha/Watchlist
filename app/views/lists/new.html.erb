<div class="container">
  <%= simple_form_for [@list] do |f| %>
  <%= f.input :name %>
  <%= f.submit "Create a new list", class: "btn btn-primary" %>
  <% end %>
  <p>
  <%= link_to "Back to all lists", lists_path %>
  </p>
</div>
